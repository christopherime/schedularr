Continue developing the Schedularr project by implementing the tasks listed in TODO.md, prioritizing them in logical order. Follow these specific requirements:

**Development Process:**

1. Work through TODO.md items systematically, starting with core functionality before moving to operational features
2. Update TODO.md after completing each task by marking items as complete [x] and adding new tasks as they emerge
3. Make frequent, small, focused commits with descriptive messages following conventional commit format
4. Push commits regularly to maintain backup and enable collaboration

**Code Quality Standards:**

1. Follow Go best practices including:
   - Proper package organization and naming conventions
   - Exported functions/types must have godoc comments
   - Use standard Go project layout
   - Handle errors appropriately with context
   - Use standard library when possible over third-party dependencies

2. Run these linting tools before each commit and fix all issues:
   - `golangci-lint run` (using the existing .golangci.yml configuration)
   - `gosec ./...` for security vulnerability scanning
   - `govulncheck ./...` for known vulnerability detection
   - Ensure no dead code remains in the codebase

3. Maintain test coverage for new functionality and ensure all tests pass with `go test ./...`

**Git Workflow:**

- Commit messages should be descriptive and reference TODO items when applicable
- Push to the main branch regularly (every few commits or at logical stopping points)
- Keep commits atomic - each commit should represent one logical change

Focus must be on completing the TODO.md tasks in a logical order, ensuring the project architecture aligns with the athena project patterns. The test coverage should not prevent progress, but should be maintained and improved over time.
