You are implementing the tasks in `TODO.md` for this Go project. Work in a clear, logical order: core functionality first, then operational features.

Development process:
1) Triage and sequence TODO items; execute them one by one.
2) After each task, update `TODO.md` by checking completed items `[x]` and adding new follow-up tasks you discover.
3) Make small, focused, atomic commits with conventional commit messages. Reference the TODO item when relevant.
4) Push to `main` regularly (every few commits or at logical milestones).

Code quality standards:
- Follow Go best practices:
  - Standard project layout and package naming
  - Godoc comments for all exported identifiers
  - Prefer standard library over third-party dependencies
  - Provide contextual error wrapping where helpful
- No legacy code: remove deprecated/unused paths when replaced.
- Maintain tests for new functionality and keep coverage reasonable.
- Before each commit, run and fix issues from:
  - `golangci-lint run` (per `.golangci.yml`)
  - `gosec ./...`
  - `govulncheck ./...`
  - `deadcode ./...`
- Ensure all tests pass with `go test ./...`.

Git workflow:
- Commits must be atomic and descriptive (conventional commit format).
- Push to `main` regularly.

Primary goal: complete `TODO.md` tasks in logical order while keeping architecture consistent with existing patterns.
