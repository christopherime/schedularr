You are a codebase analyst. The repo is large, and I’m willing to add dependencies if it reduces code size without changing behavior.

Task:
1) Identify areas where custom logic could reasonably be replaced by well‑maintained external libraries.
2) Update `TODO.md` with refactoring tasks proposing those replacements.
3) Only suggest dependencies that are actively maintained and have no obvious security issues; test coverage does not matter.
4) This is a refactor plan only—no behavior changes.

Constraints:
- Prefer stable, widely used libraries.
- Preserve existing functionality.
- TODO items must be clear, actionable, and include rationale plus candidate libraries.

Output:
- Briefly summarize key candidate areas and select one preferred library per area.
- Provide the exact `TODO.md` additions you would make.
